attrs>=17.4
autopep8
bumpversion>=0.5
check-manifest>=0.36
flake8
invoke>=0.14
isort
paramiko
pylint
pytest
scp
sphinx>=1.5
twine
m2r2
numpy
# for reading URDF joint limits in testing
compas >= 1.14 , < 2.0
# Installs the current checkout in editable mode, so use:
#   pip install -r requirements-dev.txt
# and do not combine it with an additional `pip install .`
-e .
